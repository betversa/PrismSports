name: Load NCAAB Stats (TeamRankings → Supabase)

on:
  workflow_dispatch:
  schedule:
    # Daily at 7:15 AM Chicago-ish (UTC shifts with DST).
    - cron: "15 13 * * *"

concurrency:
  group: load-ncaab-stats
  cancel-in-progress: true

jobs:
  load:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Run loader
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SEASON: "2025-26"
        run: |
          node --version
          npx ts-node scripts/teamRankingsStatsLoader.ts
